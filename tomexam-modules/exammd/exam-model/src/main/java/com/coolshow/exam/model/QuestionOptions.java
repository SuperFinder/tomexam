package com.coolshow.exam.model;

import com.coolshow.exam.common.Singleton;
import com.coolshow.exam.model.base.BaseQuestionOptions;
import com.jfinal.plugin.activerecord.Db;

import java.util.List;

/**
 * Generated by JFinal.
 */
@SuppressWarnings("serial")
public class QuestionOptions extends BaseQuestionOptions<QuestionOptions> {
  public static final QuestionOptions dao = Singleton.getInstance().getSingletonObject
      (QuestionOptions.class).dao();

  public int addOption(String salisa, int qid, String soption) {
    return Db.update(getSql("questionOptions.addOption"), salisa, qid, soption);
  }

  public int addOptions(int qid, List<String> listOfOptions) {
    return Db.update(getSql("questionOptions.addOptions"), qid, listOfOptions);
  }

  public int delete(int qid) {
    return Db.update(getSql("questionOptions.delete"), qid);
  }

  public List findById(int qid) {
    return dao.find(getSql("questionOptions.findById"), qid);
  }


  public List findInList(List lista, List listb) {

    return dao.find(getSql("questionOptions.findInList"), lista, listb);
  }


}
